const divide = function (num1 = 2000, num2 = 100) {
    let division = num1 / num2
    return division;
}
// divide(2000, 100)

const square = dimension => dimension * 2;
square(2)

const add = (a, b) => a + b;
add(3, 4)